// src/utils/createRaidPanelAssets.js
// Este arquivo foi desativado, pois a geração de GIFs foi removida.
// A funcionalidade futura carregará assets de uma CDN.

export async function createLoopedTextGif(text, color) {
    // Retorna uma promessa resolvida com null para não quebrar o fluxo que a chama.
    return Promise.resolve(null);
}

export async function createCountdownGif(textPrefix) {
    // Retorna uma promessa resolvida com null.
    return Promise.resolve(null);
}
